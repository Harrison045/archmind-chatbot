# ArchMind — Next.js frontend config
# Copy this file to `.env.local` and adjust if needed. `.env.local` is git-ignored.

# URL of the Python backend that the /api/chat route proxies to.
# Only change this if you run the backend on a different host or port.
ARCHMIND_BACKEND_URL=http://127.0.0.1:8000
